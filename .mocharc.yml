require:
  - test/setup.js
  - ts-node/register
# spec is intentionally NOT set here — each npm script (test,
# test:equivalence, test:integration) provides its own glob. A spec
# here would merge with CLI args and broaden the test set.
timeout: 10000
node-option:
  - "no-warnings"
